#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <unistd.h>
#include <string.h>
int len(char* arr);
int indexOf(char* arr, char c);
int is_correct(char* arr);
void secret_code(char a[]);
char* mystrcpy(char* destination, char* source);